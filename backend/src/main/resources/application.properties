spring.application.name=backend
server.port=8080

# Open-Meteo (sin API key requerida)
weather.api.geocoding-url=https://geocoding-api.open-meteo.com/v1
weather.api.forecast-url=https://api.open-meteo.com/v1

# Origenes permitidos para el frontend (separados por coma)
app.cors.allowed-origins=http://localhost:5173
